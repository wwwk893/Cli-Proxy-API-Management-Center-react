---
schema: ui_deliverables_v1
budget:
  ascii_max_lines: 25
  v1_max_total_lines: 160
  v2_max_total_lines: 140
  v3_max_total_lines: 140
  v4_max_total_lines: 200
---
# 提醒：`absolute_file_paths` 必须包含 `relevant_files.txt` 展开后的每个文件绝对路径（否则专家看不到代码上下文）。
