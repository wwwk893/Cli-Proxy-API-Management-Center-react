实现方案（模型排行明细悬浮展示）

## 共识方案要点（gpt-5.2-xhigh + gemini 3 pro，2025-12-22 10:34）
1) 后端必须扩展 `/api/usage/by-model`：新增 `inputTokens/outputTokens/reasoningTokens` 与 `inputCostUsd/outputCostUsd`，并用 `null` 表达“未配置价格”，前端直接显示“未配置”。  
2) 计费规则严格对齐 `lib/usage-aggregate.ts`：Codex 输入按 `(input-cached)`、输出按 `output`；Gateway 输出按 `(output+reasoning)`。  
3) 前端交互：Desktop 使用 Tooltip（hover）+ Popover（click 固定）；Touch 走 inline 展开（TableRow + Collapsible/条件渲染）。  
4) 触发器必须 `stopPropagation()`，避免触发行点击/展开冲突。  
5) 建议新增后端成本拆分 helper（如 `lib/usage-cost-breakdown.ts`），集中处理价格映射与拆分逻辑。  
6) 风险与降级：历史价格与当前价格不一致时，允许 `inputCostUsd/outputCostUsd` 为 `null`，仅展示 Token 明细。  

## 数据结构（TS 类型/接口/状态模型）
1) 扩展 by-model API 输出行（建议保持与现有字段同层级）：  
   - `inputTokens: number`  
   - `outputTokens: number`  
   - `reasoningTokens: number`  
   - `billableInputTokens: number`（已扣缓存，混合渠道用 CASE 计算）  
   - `billableOutputTokens: number`（包含 reasoning）  
   - `inputCostUsd: number | null`  
   - `outputCostUsd: number | null`  
   - `cachedCostUsd: number | null`（可选，用于提示缓存计费）  
   - `pricingConfigured: boolean`
2) 前端模型行数据结构 `ModelRow` 同步上述字段（保持兼容旧字段）。
3) 明细展示状态模型（局部）：  
   - `detailOpen: boolean`  
   - `detailPinned: boolean`（点击固定）  
   - `detailMode: "hover" | "click"`（用于触控分流）

## 文件/目录改动清单
- `app/api/usage/by-model/route.ts`：扩展 SQL 聚合字段 + 价格拆分计算
- `lib/pricing-map.ts`：复用 loadPricingMap（无需改动或增加小辅助）
- `lib/api/schemas.ts`：补充 by-model 返回类型/前端推断类型（如有）
- `components/charts/usage-model-table.tsx`：模型行接入明细触发器与展示
- `components/charts/model-usage-detail.tsx`（新建）：明细内容渲染组件
- `components/charts/model-usage-detail-trigger.tsx`（新建）：hover/click 触发器
- `components/i18n-context.tsx`：新增文案 key

## 组件拆分与职责边界
- `ModelUsageDetail`：只负责渲染明细内容（Token + 价格），不处理触发逻辑。
- `ModelUsageDetailTrigger`：处理 hover/click/触控兼容、阻止冒泡、aria 标注。
- `UsageModelTable`：保留表格与排序/展开逻辑；模型列内嵌触发器与明细。
- >600 行策略：若 `usage-model-table.tsx` 接近阈值，将明细触发器与内容抽离到独立文件（如上）。

## 分步实现 checklist
- [x] 在 `app/api/usage/by-model/route.ts` 增加 `inputTokens/outputTokens/reasoningTokens` 聚合（Daily + Event 两套 SQL）。
- [x] 在 `app/api/usage/by-model/route.ts` 使用 `loadPricingMap()` 计算 `inputCostUsd/outputCostUsd/cachedCostUsd` 与 `pricingConfigured`。
- [x] 使用 CASE 计算 `billableInputTokens/billableOutputTokens`：Codex 走扣缓存 + 输出不含 reasoning；Gateway 输出含 reasoning。
- [x] 更新 `components/charts/usage-model-table.tsx` 的 `ModelRow` 类型，接入明细字段。
- [x] 新建 `components/charts/model-usage-detail.tsx`：明细网格（输入/输出/思考 + 金额），未配置显示“未配置”。
- [x] 新建 `components/charts/model-usage-detail-trigger.tsx`：hover 预览 + click 固定（触控点击展开）。
- [x] 在 `usage-model-table.tsx` 模型列插入触发器，并确保不影响行点击/展开。
- [x] 在 `components/i18n-context.tsx` 补齐文案（输入/输出/思考/未配置/计入输出）。
- [x] 评审加固：费率合法性校验，异常时明细置空（`app/api/usage/by-model/route.ts`）。
- [x] 评审加固：明细金额渲染 NaN/Infinity 兜底为“未配置”（`components/charts/model-usage-detail.tsx`）。
- [x] 评审加固：`costUsd` 标准化为 number（`components/charts/usage-model-table.tsx`）。
- [x] 评审加固：Popover 打开时禁用 Tooltip 明细（`components/charts/model-usage-detail-trigger.tsx`）。
- [x] 评审加固：模型行展开 aria-label 走 i18n（`components/charts/usage-model-table.tsx`、`components/i18n-context.tsx`）。
- [ ] 本地手动验证并记录结果。

## 验证 checklist（手动）
- [ ] hover/click 均可看到明细，触控设备点击可展开。
- [ ] 输入/输出/思考 Token 与当前筛选范围一致。
- [ ] 输入金额 + 输出金额 ≈ 成本（允许极小误差）。
- [ ] Codex 规则：输入金额按 (input - cached)；输出金额不含 reasoning。
- [ ] Gateway 规则：输出金额含 reasoning。
- [ ] 未配置价格时显示“未配置”，页面不崩溃。
- [ ] 明细触发不影响行点击与展开来源行。
- [ ] 明细按钮打开时 Tooltip 不遮挡 Popover。
- [ ] 展开按钮 aria-label 在中英文切换下正确。

## 风险与回滚
- 风险1：混合渠道时计费拆分逻辑错误 → 回滚到仅显示 token 明细或只显示总成本。
- 风险2：旧数据缺 reasoning/cached → 使用 `0` 兜底并提示。
- 风险3：Popover 被表格容器遮挡 → 使用 portal/调整 z-index 或改为 inline 展开。
- 回滚：撤销 by-model 字段扩展与前端明细组件，恢复原表格。

## 主人确认
- 主人确认：
  - 状态：已确认
  - 记录：2025-12-22 10:38 主人确认实现方案，但暂不落地实现代码
  - 记录：2025-12-22 10:40 主人同意按本方案执行并开始落地实现代码
