---
schema: prompt_pro_v1
task:
  name: "控制台概览页统计看板"
  dir: "feature/控制台概览页统计看板"
batch:
  id: "0"
meta:
  timestamp: "2026-01-04 10:27"
  target_model: "gpt-5.2-pro"
outputs:
  file: "plan.pro.md"
notes:
  - "请把本文件内容原样复制到 ChatGPT（GPT‑5.2 Pro）的对话里。"
  - "同时上传同目录生成的 bundle zip（里面有 review-package.md、relevant_files.txt、context/）。"
---

# 你是架构/算法设计专家（GPT‑5.2 Pro）

你将收到一个 zip 证据包（bundle-*.zip）。请你先在 zip 内找到并阅读：
- `review-package.md`：需求/范围/验收/约束/现状
- `relevant_files.txt`：需要重点关注的文件绝对路径清单
- `context/`：上述 relevant_files 的快照（按路径复制）

你的任务：输出一份**可直接照着执行的“施工图方案”**到 `plan.pro.md`（只输出 Markdown 内容即可）。

## 强约束（必须遵守）
- 全中文
- 不要输出“泛泛建议”，必须给出**可落地**的结构、数据模型、算法流程与步骤
- 必须逐条对齐 `review-package.md` 的验收标准与约束（不对齐要说明理由与替代）
- 所有关键建议都要带定位：
  - `文件路径` + `组件/函数/类型名`（如果是新增，也要给建议路径与命名）
- 允许写少量关键伪代码/代码骨架（接口/类型/关键算法），但禁止粘贴整页大组件实现
- 若信息不足：只列 **最小缺失信息清单**（<= 6 条），并给出你在缺信息情况下的保守默认假设

## 输出格式（请严格按此结构）
请按以下章节输出（标题必须一致）：

# Plan.Pro — 控制台概览页统计看板 / Batch 0

## 0. 结论摘要（给执行者看的 TL;DR）
- 目标与范围：
- 核心改动点（<= 8 条）：
- 关键风险与兜底（<= 6 条）：

## 1. 总体架构与数据流
- 模块划分（职责边界）
- 端到端数据流（从入口到 API/缓存/状态再到 UI）
- 关键时序（用文字步骤或简短伪代码）

## 2. 数据结构与状态模型（必须尽量完整）
> 输出 TypeScript 类型/接口（或等价的强类型定义），包含：
- 关键实体（DTO/VO/Model）
- 页面状态（loading/empty/error/权限/分页/筛选）
- 表单/编辑态（dirty/校验/提交/回滚）
- 缓存键/索引（如果有）

## 3. 接口与副作用（API/Service/Hook）
- API 列表（路径/方法/入参/出参/错误码假设）
- Service 层职责（请求、重试、取消、并发、缓存）
- 副作用与一致性策略（竞态、刷新、轮询、WebSocket 等）

## 4. 文件/目录改动清单（尽量到文件级）
- 新增：
- 修改：
- 删除（如有）：
> 每条都写“为什么改”与“涉及的关键 symbol”。

## 5. 组件拆分与 >600 行策略
- 哪些文件可能超 600 行？如何拆（子组件/Hook/Utils/Types）
- 拆分后的依赖方向（避免循环依赖）

## 6. 分步实现 Checklist（尽量详细，可直接照做）
> 使用 `- [ ]`，每一项必须注明涉及文件路径。
- [ ] ...

## 7. 验证 Checklist（手动验证 + 边界 + 回归）
> 使用 `- [ ]`，并尽量给具体操作与预期结果。
- [ ] ...

## 8. 风险与回滚（必须可操作）
- 风险点 → 触发条件 → 监控/发现方式 → 回滚步骤
- 需要 Feature Flag 吗？如何默认关闭/灰度？

## 9. 最小缺失信息（若需要）
- ...

（结束）
